<div class="sessions-new-wrapper">
  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4 sessions-new-container">
        <h2 class="text-center">発注者ログイン</h2>
        <%= form_for :session, url: login_customer_path do |f| %>
          <div class="form-group">
            <%= f.label :メールアドレス, class: "" %>
            <%= f.text_field :email, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :パスワード, class: "" %>
            <%= f.password_field :password, class: "form-control" %>
          </div>
          <%= f.label :remember_me, class: "checkbox inline" do %>
            <%= f.check_box :remember_me %>
            <span>次回から自動でログイン</span>
          <% end %>
          <%= f.submit "ログイン", class: "btn btn-block active" %>
        <% end %>
        <%= link_to "発注者登録はこちら", new_customer_path %>
        <%= link_to "アカウントロックの解除はこちら", new_release_account_lock_path %>
      </div>
    </div>
  </div>
</div>
